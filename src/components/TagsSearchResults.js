import React, { Component } from 'react';
import RecipeCard from './RecipeCard';
//import { Grid, Row, Col, Clearfix } from 'react-bootstrap';
var hello = "hello"

class TagsSearchResults extends Component {
  render() {
    return (
      <div>
        {hello}
      </div>
    );
  }
}

export default TagsSearchResults;